{
  "name": "dentalmart",
  "version": "1.0.0",
  "private": true,
  "description": "DentalMart static catalogue site — products.csv in, static SEO pages out.",
  "scripts": {
    "build": "node build.js",
    "preview": "node build.js && cd dist && python3 -m http.server 8080"
  }
}
